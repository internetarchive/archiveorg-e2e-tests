name: Pull Request

on:
  pull_request:
    branches: [ main ]

jobs:
  install:
    uses: ./.github/workflows/install.yml

  run-e2e-tests:
    needs: install
    uses: ./.github/workflows/run-e2e-tests.yml
