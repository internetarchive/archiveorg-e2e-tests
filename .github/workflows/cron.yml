name: Automated

on:
  push:
  schedule:
    - cron: "*/17 * * * *" # every 17 mins

jobs:
  lint-n-circular:
    uses: ./.github/workflows/check-lint-circ.yml

  music:
    uses: ./.github/workflows/music.yml

  books:
    uses: ./.github/workflows/books.yml

  bookreader:
    uses: ./.github/workflows/bookreader-e2e-tests.yml
